<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orderbuzz.vendor"
    android:versionCode="1"
    android:versionName="0.0.1-SNAPSHOT" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
           <activity android:name="com.orderbuzz.vendor.PendingOrderListViewActivity" />
           <activity android:name="com.orderbuzz.vendor.DoneOrderListViewActivity" />
           <activity android:name="com.orderbuzz.vendor.PendingOrderSummaryListViewActivity" />
           <activity android:name="com.orderbuzz.vendor.OrderTabAndListView"/>
        
        
    </application>
    
    
    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>